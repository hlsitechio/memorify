# Frontend (public-safe only — never put secrets in VITE_*)
# VITE_CLERK_PUBLISHABLE_KEY=pk_live_...

# Backend secrets live ONLY in Netlify env / backend/.env.local (gitignored):
# NEON_DATABASE_URL=
# MEMORIFY_AGENT_TOKEN_SECRET=
# CLERK_SECRET_KEY=
# CLERK_PUBLISHABLE_KEY=
# CLERK_FRONTEND_API_URL=
